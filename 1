import Vue from "vue";
import VueRouter from "vue-router";

import AppLayout from "./components/AppLayout.vue";

Vue.use(VueRouter);

const routes = [{ path: "/" }, { path: "/layout", component: AppLayout }];

export default new VueRouter({ mode: "history", routes });
